# Runtime dependencies for the playable web demo (the classical alpha-beta engine
# in search.py). This is what Vercel and Render auto-install -- intentionally
# torch-free and tiny so it fits a serverless function / free instance.
python-chess>=1.11
fastapi>=0.110
uvicorn>=0.27

# To TRAIN the AlphaZero neural network as well, also install the heavier extras:
#   pip install -r requirements.txt -r requirements-train.txt
