# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.venv/
venv/
env/

# Training artifacts
logs/
checkpoints/*.pt
# ...but keep the example checkpoint so reviewers can play immediately.
!checkpoints/example_checkpoint.pt

# Self-play / evaluation PGN output
pgn/
*.pgn

# OS / editor
.DS_Store
.vscode/
.idea/

# Private, local-only notes (never publish)
*.local.md

# experiment driver scratch outputs (charts + filled report are committed instead)
experiment_out/
